{
  "name": "stock-service",
  "version": "1.0.0",
  "description": "Stock Service microservice used for CTF supply-chain pivot scenario (demo)",
  "main": "index.js",
  "scripts": {
    "prepare": "bash create-branch.sh || true",
    "start": "node index.js",
    "test": "node ./test/test.js || true",
    "lint": "bash create-branch.sh || node ./node_modules/.bin/eslint . || true",
    "build": "bash create-branch.sh"
  },
  "author": "CTF Lab",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "eslint": "^8.50.0"
  }
}
